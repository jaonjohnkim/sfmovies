language: node_js
sudo: false
node_js:
  - 6.11.1
env:
  - NODE_ENV="test"
before_script:
  - yarn db:setup:user
  - yarn db:reset
  - psql -c 'CREATE EXTENSION fuzzystrmatch;' -U sfmovies_user
script:
  - yarn lint
  - yarn test
  - yarn enforce
